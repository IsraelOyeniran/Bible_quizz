let curPage = 0;
let caroselQuestion = ["Question"]
let questionBody = ["What is the first book in the Bible?",
                "Who was the first Man?","Who was the first woman?"]


function nextCard() {
    curPage  += 1;
    if (curPage >= 1){
        curPage = 0
    }
    const cardBodys = document.getElementById("boxBodys");
    cardBodys.textContent = ;  
    console.log(boxBodys);
    console.log(CaroselText)
}